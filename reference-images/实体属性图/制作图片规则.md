# 实体属性图制作规则

- 先参考本目录 `实体属性图.png` 和 `实体属性图.drawio` 的中心实体、属性分布、连线和配色。
- 中心实体名称应与数据库表名、实体类名或论文实体名一致。
- 属性必须来自真实字段，不补充数据库或源码没有的字段。
- 属性节点围绕中心实体均匀排布，连线不得重叠属性文字或互相覆盖。
- 字段超过 10 个时优先展示主键、外键、名称、状态、时间、业务核心字段。
- 属性名称尽量短，必要时使用中文字段含义，不写字段解释长句。
- 导出前检查字段是否真实、连线是否清晰、文字是否能看清。
