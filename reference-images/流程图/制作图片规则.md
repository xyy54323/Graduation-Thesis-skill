# 流程图制作规则

- 先参考本目录 `流程图.png` 和 `流程图.drawio` 的节点形状、正交流向、判断分支和线条间距。
- 开始和结束使用圆角矩形，处理步骤使用矩形，判断使用菱形。
- 流程方向优先自上而下；复杂流程可局部分支，但主线必须清楚。
- 判断分支必须标注“是/否”或具体条件，分支线从菱形不同方向引出。
- 连线必须使用正交线，不能穿过节点、文字或箭头；回流线应绕外侧走。
- 节点文字使用短语，不写大段说明；多个动作不要挤在一个节点中。
- 线条交叉或覆盖时必须调整布局、增加折点或拆成多张流程图。
