# 用例图制作规则

- 先参考本目录 `用例图.png` 和 `用例图.drawio` 的参与者位置、系统边界、用例椭圆和连线样式。
- 参与者放在系统边界外侧，用例放在系统边界内侧。
- 用例名称应来自真实角色入口和功能模块，不写项目未实现功能。
- 参与者与用例连线尽量短且不交叉；连线不能覆盖用例椭圆文字。
- include 和 extend 关系只在确有复用或扩展关系时使用，不为了复杂而添加。
- 用例过多时拆成管理员端、用户端、移动端或硬件端用例图。
- 导出前检查参与者、用例、系统边界、include/extend 方向是否清楚。
