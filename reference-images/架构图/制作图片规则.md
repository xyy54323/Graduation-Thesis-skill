# 架构图制作规则

- 先参考本目录 `架构图.png` 和 `架构图.drawio` 的分层布局、蓝灰配色、节点大小和连线方式。
- 按项目真实结构绘制前端、后端、移动端、数据库、第三方服务或硬件设备，不添加源码中没有的缓存、消息队列、集群等组件。
- 优先使用上下分层或左右分层布局，同一层节点对齐，层间留足空间。
- 连线使用正交线或短直线，避免斜穿节点；线条不得覆盖文字、图标或边框。
- 箭头方向表示调用、访问或数据流向，含义要在图中保持一致。
- 组件过多时拆成总体架构图和模块关系图，不强行塞进一张图。
- 导出前检查所有线条是否穿过节点、是否交叉覆盖、文字是否清晰。
