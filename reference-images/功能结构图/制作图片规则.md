# 功能结构图制作规则

- 先参考本目录 `功能结构图.png` 和 `功能结构图.drawio` 的树状结构、层级间距、字体和线条风格。
- 功能来源必须来自菜单、路由、页面、控制器、需求说明或用户确认材料。
- 通常控制在 2 到 3 层：系统名称、一级模块、二级功能；不要把所有页面按钮都列入图中。
- 父子关系线使用正交连接，不能穿过模块框或文字。
- 一级模块数量过多时按管理员端、用户端、移动端、AI 模块或硬件端拆分。
- 模块名称要短，避免长句；同层节点尺寸尽量一致。
- 导出前检查层级是否清楚、连线是否重叠、功能是否与正文一致。
