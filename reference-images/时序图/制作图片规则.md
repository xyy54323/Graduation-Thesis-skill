# 时序图制作规则

- 先参考本目录 `时序图.png` 和 `时序图.drawio` 的对象排列、消息线、返回线和时间顺序。
- 参与对象从左到右排列，通常包含页面、接口、控制器、服务、数据库或外部服务。
- 消息从上到下表示时间顺序，不能把时序图画成普通流程图。
- 调用名称应对应真实页面行为、接口路径、控制器方法或外部服务调用。
- 消息线和返回线不能覆盖文字；相邻消息之间留出阅读空间。
- 对象过多或流程过长时拆成核心业务时序图和外部服务调用时序图。
- 导出前检查调用顺序、对象名称、返回结果和线条方向是否清楚。
