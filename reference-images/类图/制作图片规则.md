# 类图制作规则

- 先参考本目录 `类图.png` 和 `类图.drawio` 的类框样式、蓝色标题栏、灰色类体和虚线依赖关系。
- 只画核心类、组件、控制器、服务、实体或关键工具类，不画无关页面说明框。
- 类框中只保留核心属性和核心方法，避免把源码完整字段全部搬进图中。
- 关系线应区分依赖、关联、继承或实现，箭头方向要符合调用或继承方向。
- 类框之间保持间距，关系线不得穿过类框文字；关系过多时拆成多个局部类图。
- 类名、方法名和属性名应来自真实源码；没有源码证据时不写。
- 导出前检查类名、方法名、关系方向和线条是否清晰。
